  <div class="banner d-flex align-items-center" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://images.unsplash.com/photo-1544957992-20514f595d6f?q=80&w=2070&auto=format&fit=crop&ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D);">
    <div class="container text-center" >
      <h1>CIRCLE</h1>
      <p>The circular fashion renting experience</p>
    </div>
  </div>

<h2>Products</h2>

<div class="mt-5 row my-2 d-flex flex-wrap">
  <% @items.each do |item| %>
    <div class="col-sm-12 col-md-6 col-lg-3 mb-4">
      <div>
        <%= link_to item_path(item) do %>
          <%= cl_image_tag item.photo.key, class: 'image-cover', height: 250 %>
        <% end %>
        <div class="mt-4">
          <h5 class="card-title"><%= item.name %></h5>
          <p id="hidde-description" class="card-description"><%= item.description %></p>
        </div>
        <div>
          <p class="card-price">Rent from: <%= item.price %> EUR / day</p>
          <%= link_to "Details", item_path(item), class: "btn btn-dark rounded-4 mb-3" %>
        </div>
      </div>
    </div>
  <% end %>
</div>
